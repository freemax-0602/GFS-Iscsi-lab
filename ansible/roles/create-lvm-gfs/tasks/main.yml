---

- name: create lvm on master-node
  block:
  # Check new vg_root
    - name: Check vg
      shell: "vgdisplay -v {{ vg_name }}"
      register: "vg_check"
      changed_when: false
      failed_when: false
      check_mode: no

    # # Physical volume create
    # - name: Create volume group
    #   lvg:
    #       vg: "{{ vg_name }}"
    #       pvs: /dev/sdb #TODO-диск может быть другой
    #       state: present
    #       clustered: yes
    #   when: vg_check.rc != 0 

    # # Check logical volume 
    # - name: Check lvc
    #   shell: "lvdisplay {{ lv_dir }}"
    #   register: "lv_check"
    #   changed_when: false
    #   failed_when: false
    #   check_mode: no

    # # Create logical valume
    # - name: Create logical volume
    #   lvol:
    #       vg: "{{ vg_name }}"
    #       lv: "{{ lv_name }}"
    #       force: yes
    #       size: 100%FREE
    #   when: lv_check.rc != 0

#     # Formating xfs filesystem
#     - name: Format the xfs filesystem
#       filesystem:
#         fstype: xfs
#         dev: "{{ lv_dir }}"
#   when: role == "mater-node"

# - name: mounting logical volume
#   block:
#     # Mounting logical volume
#     - name: Mounting logical volume on /mnt
#       mount:
#         path: "{{ mnt_dir }}" #TO-DO: выбпать из скрипта
#         src: "{{ lv_dir }}"
#         fstype: "{{ fs_type }}"
#         state: mounted

#     # Check /mnt
#     - name: /mnt check
#       shell: "ls {{ mnt_dir }}"
#       register: "check_mnt"
#       changed_when: false
#       failed_when: false
#       check_mode: no
